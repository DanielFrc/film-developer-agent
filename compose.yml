services:
  etl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: film_dev_agent
    environment:
      - DATA_PATH=/data
    volumes:
      - ./data:/data
    command: python entrypoint.py

volumes:
  film_dev_data:

# API base URL for the typed client.
# Use /api in dev to hit Vite proxy → backend (see vite.config.ts).
VITE_API_URL=/api

# Proxy target when using /api (optional override)
# VITE_API_PROXY_TARGET=http://localhost:8000
